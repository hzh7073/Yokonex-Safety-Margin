# 役次元 · 画地为牢

**好玩的创意，欢迎一起用。**

这是役次元的单人人体边界挑战游戏。玩家通过手机摄像头画出活动区域，在规定时间内保持身体关节不越界；持续越界或离开画面后，游戏会记录触发事件，并可通过蓝牙连接 EMS 设备输出预设波形。

我们把它开源，让玩家、开发者和同行都能拿去研究、修改，共同创造“自信自爱，方得自由”的役次元社区。

## 玩法

### 画地为牢 · 单人边界挑战

固定手机并确保全身进入画面，使用圈画或矩形工具划定活动区域。游戏开始后，应用会实时识别人体骨骼，并判断肩、肘、腕、髋、膝、踝等关节是否仍在区域内。

玩家可以设置游戏时长、异常持续时间和重复触发间隔。连接兼容的 EMS 设备后，还可以选择设备代际、输出通道、波形和强度。

<p align="center">
  <img src="docs/images/game-interface.jpg" alt="画地为牢实际游戏界面" width="640">
</p>

## 使用流程

1. 授予摄像头权限，固定手机，确保全身进入画面。
2. 选择圈画或矩形，画出活动区域。矩形可拖动四角调整，切换摄像头后需要重新画区。
3. 设置游戏时长、异常持续时间和重复触发间隔。
4. 在 EMS 设备页选择设备代际、通道、波形和强度，并连接设备。
5. 确认电极片已正确接入后开始游戏。
6. 游戏结束后查看本局触发记录，或直接再来一局。

默认游戏时长为 5 分钟，异常持续时间为 3 秒，重复触发间隔为 3 秒。游戏时长支持小数分钟，其余时间使用整数秒。

## 游戏模式

所有模式都会在摄像头画面中显示 ML Kit 实时识别到的人体骨架与可靠关节点。游戏中的动作要求、红绿灯切换、识别异常、倒计时、暂停和触发警告会以大字显示并通过系统文字转语音播报；提示只在内容或状态变化时播报，不会随每个识别帧重复播放。手机需启用系统文字转语音引擎并调高媒体音量。

准备页的“游戏模式”菜单保留原有经典安全区，并提供以下玩法：

- **木头人**：提示音乐播放时可以移动，音乐停止后根据骨骼点位移检测身体是否静止；移动和静止时间均可自定义。
- **缩圈模式**：从玩家画出的区域开始，按本局进度平滑缩小到原尺寸的 45%。
- **指定姿势**：随机要求举手、抬腿、下蹲或展开双手，保持成功后得分并更换动作。
- **闪避模式**：摄像头画面中显示往返移动的红色禁区，左右身体碰到时分别记录方向。
- **平衡挑战**：双手展开并单脚站立，结果页记录最长坚持时间。
- **连击模式**：连续完成随机动作累加连击和分数；失败会中断当前连击。
- **双区模式**：画出的区域自动分成 A/B 两区，身体左侧违规强制映射 A 通道，右侧违规强制映射 B 通道。
- **自定义姿势**：在实时摄像头画面上拖动头、肩、肘、腕、髋、膝、踝，绘制目标火柴人。编辑时会用绿色/红色骨架提示身体是否对齐，但绝不会触发设备输出；开始后头、躯干和四肢持续未对齐超过设定秒数才触发。打开“随机变化姿势”后，可选择全身随机、双手背后、双手合并举高或双手左右抬高四种策略，内置 32 个姿势变体，并可按固定 1 至 300 秒或随机区间自动换姿势。

木头人右侧的设置按钮可分别调整“音乐可移动时间”和“静止时间”（1 至 60 秒）。打开“每轮随机时间”后，每轮会分别在 1 秒到所设上限之间重新随机。自定义姿势右侧的设置按钮用于编辑火柴人，并可把允许不匹配时间设为 1 至 30 秒；随机姿势设置和自定义骨架都会保存在本机。

经典、缩圈和双区模式需要先画区，其余模式识别到完整人体后即可开始。所有模式共用同一套设备强度上限、Cooldown、急停和断线清零逻辑；得分、连击和缩圈进度绝不会自动提高设备强度。

功能扩展前的稳定版本保存在 Git 分支 `backup/dglab-coyote-v1` 和标签 `dglab-coyote-v1.0.0`，两者均指向提交 `ab64ce7`。

## DG-LAB Coyote 3.0 快速开始

1. 准备好郊狼 3.0、电极片和手机，并在手机上打开最新版 DG-LAB 官方 App。
2. 打开 Yokonex Safety Margin，进入右上角的设备设置。
3. 将输出设备切换为 `DG-LAB Coyote`，连接方式保留默认的“我的专用服务器”，点击“连接郊狼”。程序会连接预置的专用 WSS 中转并显示二维码，不需要输入 IP。
4. 使用 DG-LAB App 扫描二维码，等待页面依次显示“App 已连接”和“已连接”。页面会列出发现的 Coyote 3.0、电量以及 A/B 实时强度。
   如果两款 App 安装在同一台 Android 手机上，可点击二维码下方的“在本机 DG-LAB App 中连接”，无需扫描自己的屏幕。
5. 选择 A、B 或 A+B 通道和波形。首次使用建议保留触发强度 `5`、最大允许强度 `20`、持续时间 `1000 ms` 和 Cooldown `3 s`。
6. 佩戴并确认电极片连接正确后，主动点击“低强度测试”。测试强度最高为 `3`，持续时间最高为 `500 ms`，并受 Cooldown 限制。
7. 保存设置，返回主页并开始 Safety Margin。越界事件会按所选通道和波形发送有限时长的输出。
8. 需要停止时点击红色“立即停止”；游戏运行页也提供红色急停图标。急停会清空 A/B 已执行和排队的任务并把两个通道归零。

连接异常时，先确认手机可以访问网络、DG-LAB App 已连接郊狼 3.0，并让两个设备保持联网。二维码等待超过 5 分钟会由中转服务关闭，点击“重新连接”即可生成新会话。App、设备或 WebSocket 断开后不会自动恢复上一次输出；必须重新连接并确认安全设置。

郊狼设置提供以下连接方式：

- **我的专用服务器（推荐）**：预置 `wss://38.244.4.154.sslip.io:18444/dglab-v4`，不要求两台手机处于同一局域网；地址也可以在 UI 中修改。
- **官方网络服务**：连接 DG-LAB 官方 V4 Relay。
- **局域网直连（两台设备）**：应用自动在当前手机内启动 V4 Relay 并生成 `ws://局域网地址` 二维码。运行本应用的手机与运行 DG-LAB App 的手机需要连接同一 Wi-Fi，或让其中一台开启热点；不需要手动输入 IP、端口或启动额外服务。
- **本机回环（同一台手机）**：两款 App 安装在同一台手机时使用，二维码指向 `ws://127.0.0.1`。连接过程中不要强制结束本应用，并允许它在后台运行。

局域网模式如果扫码后一直等待，请关闭访客 Wi-Fi/AP 隔离，确认两台手机确实处于同一网段，并允许系统的“本地网络/附近设备”权限。内嵌 Relay 的会话 ID 已与官方实现统一为 8 位十六进制。切换连接方式会先停止 A/B 输出并关闭旧会话，不会恢复之前的任务。

> 扫码或连接不会触发输出。每次新连接都会先清空任务并将 A/B 强度归零。所有实际输出统一受“最大允许强度”限制，程序不会自动提高强度。

开启“按越界侧映射 A/B”后，左侧监测关节越界发送到 A，右侧发送到 B，两侧同时越界发送到 A+B；无人或遮挡等无法判断侧别的事件仍使用上方选定通道。结果页会显示累计异常时间、最长安全时段以及每次触发的方向和持续时间。

## 模拟输出（无需设备）

没有 Yokonex 或 Coyote 时，可在设备设置中选择“模拟输出”。该模式不连接蓝牙、不启动 WebSocket，也不会向任何硬件发送指令；它会在页面上显示模拟脉冲、通道、波形、强度和剩余时间，用来验证游戏规则和触发链路。点击“模拟测试”只会记录一条低强度、短时长的屏幕事件，点击“立即停止”可清除当前模拟输出；模拟配置与真实郊狼配置分别保存在本机。

## 判断规则

- 左右肩、肘、腕、髋、膝、踝共 12 个关节参与判断，可信度阈值为 `0.6`，边界上的关节视为区域内。
- 任一可信关节越界，或持续识别不到人，都会开始连续异常计时；两种异常状态互相切换时不会清零。
- 达到异常持续时间后首次触发，异常继续时按照重复触发间隔再次触发。
- 完整识别到人体回到区域内后，异常计时清零。
- 识别到人体但关节不完整，且没有可信越界点时，游戏自动暂停；完整归位后可手动继续。
- 手动暂停、应用进入后台、摄像头故障时，异常计时清零，暂停时间不计入游戏时长。
- 识别流超过 1.5 秒没有新结果时，视为识别中断并暂停游戏。
- 游戏结束优先于同一时刻的设备触发，程序卡顿后不会集中补发触发事件。
- 参数和区域保存在本机，区域与当前摄像头绑定；触发记录仅保留当前一局。

## DG-LAB 实现依据

本项目按照 DG-LAB 官方 `dglab-kit` V4 Socket 协议实现，最初核对版本为 `1.0.5`、提交为 `e1707e6e7ef50bdbe8c8dafe19f34289e01dffc0`，并继续对照官方当前维护的 `dglab-kit` 与 `dglab-websocket-server` V4 文档。应用可连接专用 Relay、官方 `wss://trex.dungeon-lab.cn/v4`，也可在进程内启动兼容的局域网/回环 Relay；配对二维码使用官方 `dungeon-lab.cn/s` Socket action 格式。服务器部署包位于 `deploy/dglab-relay`，固定使用官方服务端提交 `497645525a9ec4e159587d3f2c463d7399ba17e5`。实现覆盖设备快照/增量、临时强度、V3 波形帧、任务清理和连接状态。

## 好玩的点子，值得有更多版本

画地为牢从一个想法变成了可以玩的游戏，现在，我们把代码也一起分享出来。

喜欢这个玩法，欢迎直接拿去用；有自己的想法，欢迎在它的基础上继续改。玩家、开发者、同行，都可以从这里开始。

**这份玩法，大家尽管用。役次元继续琢磨下一份心动。**

## 免费开源，允许商用，友商也欢迎

本项目采用 [MIT 许可证](https://opensource.org/license/mit)。简单说，你可以：

- 免费使用、复制和分享本项目。
- 修改代码，做成自己的版本。
- 用在商业产品、收费项目或线下经营活动中。
- 作为友商，把代码用进自己的产品，无需另行向我们申请授权。

## KTV、酒吧需要？可以免费聊聊

如果你经营 KTV、酒吧，想把画地为牢用在店里，欢迎联系我们，我们可以提供免费的咨询服务。

## 开始体验

游戏体验或下载：https://github.com/YCY-YOKONEX/Yokonex-Safety-Margin/releases

欢迎来玩，也欢迎拿去做出更好玩的版本。
